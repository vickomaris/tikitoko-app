// global state for auth
const initialState = {
    
};

const authReducer = (state = initialState, action) => {
    // register buyer
    // login buyer
    // register seller
    // login seller
    return state;
};

export default authReducer;