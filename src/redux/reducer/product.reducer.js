// global state for product
const initialState = {
    
};

const productReducer = (state = initialState, action) => {
    // get product
    // insert product
    // update product
    // delete product
    return state;
};

export default productReducer;