// global state for address
const initialState = {
    
};

const addressReducer = (state = initialState, action) => {
    // get address
    // insert address
    // update address
    // delete address
    return state;
};

export default addressReducer;