// global state for cart
const initialState = {
    
};

const cartReducer = (state = initialState, action) => {
    // get cart
    // insert cart
    // update cart
    // delete cart
    return state;
};

export default cartReducer;