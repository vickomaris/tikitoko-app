// global state for user
const initialState = {
    
};

const userReducer = (state = initialState, action) => {
    // get user
    // update user
    // delete user
    return state;
};

export default userReducer;